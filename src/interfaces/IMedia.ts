export interface IImage {
    public_id: string;
    version: string;
    secure_url: string;
}

export interface img {
    data: string;
    fileName: string;
}

export type Image = {
    public_id: string,
    version: string,
    secure_url: string,
}